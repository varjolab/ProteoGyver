readRenviron("/etc/default/locale")
LANG <- Sys.getenv("LANG")
if(nchar(LANG))
   Sys.setlocale("LC_ALL", LANG)
install.packages('BiocManager')
BiocManager::install("impute")
BiocManager::install("pcaMethods")
BiocManager::install("vsn")
install.packages(c('proteomicsCV', 'imputeLCMD', 'IRkernel', 'devtools', 'tidyverse', 'flashClust', 'WGCNA', 'samr'), ncpus=12)
library(devtools)
install_github("https://github.com/vdemichev/diann-rpackage")
BiocManager::install('SummarizedExperiment')
BiocManager::install('MSnbase')
BiocManager::install('DEP')
IRkernel::installspec(user = FALSE) 

